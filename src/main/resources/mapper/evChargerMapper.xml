<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.ggshop.v1.mapper.EvChargerMapper">
    <insert id="insertEvCharger" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_ev_charger (
                                    ev_charger_address,
                                    ev_charger_uid,
                                    ev_charger_content,
                                    ev_charger_status,
                                    ev_charger_mod,
                                    setup_date,
                                    company_id
        ) values (
                     #{evChargerAddress},
                     #{evChargerUid},
                     #{evChargerContent},
                     #{evChargerStatus},
                     #{evChargerMod},
                     #{setupDate},
                     #{companyId}
                 )
    </insert>


    <select id="selectEvChargerList"
            resultType="com.app.ggshop.v1.domain.EvChargerVO">
        SELECT
            id,
            ev_charger_address,
            ev_charger_uid,
            ev_charger_content,
            ev_charger_status,
            ev_charger_mod,
            setup_date,
            created_date,
            updated_date,
            company_id
        FROM tbl_ev_charger
        WHERE company_id = #{companyId}
        ORDER BY id DESC
    </select>

    <select id="selectAllEvChargers"
            resultType="com.app.ggshop.v1.domain.EvChargerVO">
        SELECT
            id,
            ev_charger_uid,
            ev_charger_address,
            ev_charger_content,
            ev_charger_status,
            ev_charger_mod,
            setup_date,
            created_date,
            updated_date,
            company_id
        FROM tbl_ev_charger
        ORDER BY id DESC
    </select>





    <select id="selectAll" resultType="com.app.ggshop.v1.dto.EvChargerDTO">
        SELECT
            id,
            ev_charger_uid AS stationNumber,
            ev_charger_address AS installAddress,
            ev_charger_content AS chargerNote,
            ev_charger_status,
            ev_charger_mod,
            setup_date AS installDate,
            created_date,
            updated_date,
            company_id AS companyId
        FROM tbl_ev_charger
        ORDER BY id DESC
        LIMIT #{rowCount} OFFSET #{offset}
    </select>

    <select id="selectTotalCount">
        select count(*) from tbl_ev_charger
    </select>

    <!-- 검색 결과 조회 (페이징) -->
    <select id="selectBySearch" resultType="com.app.ggshop.v1.dto.EvChargerDTO">
        SELECT
        id,
        ev_charger_uid AS stationNumber,
        ev_charger_address AS installAddress,
        ev_charger_content AS chargerNote,
        ev_charger_status,
        ev_charger_mod,
        setup_date AS installDate,
        company_id AS companyId
        FROM tbl_ev_charger
        <where>
            <choose>
                <!-- 전체 검색 -->
                <when test="searchType == 'all'">
                    (ev_charger_uid LIKE CONCAT('%', #{keyword}, '%')
                    OR ev_charger_address LIKE CONCAT('%', #{keyword}, '%'))
                </when>
                <!-- 충전소번호 검색 -->
                <when test="searchType == 'stationNumber'">
                    ev_charger_uid LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <!-- 주소 검색 -->
                <when test="searchType == 'address'">
                    ev_charger_address LIKE CONCAT('%', #{keyword}, '%')
                </when>
            </choose>
        </where>
        ORDER BY id DESC
        LIMIT #{criteria.rowCount} OFFSET #{criteria.offset}
    </select>

    <!-- 검색 결과 총 개수 -->
    <select id="selectTotalBySearch" resultType="int">
        SELECT COUNT(*)
        FROM tbl_ev_charger
        <where>
            <choose>
                <when test="searchType == 'all'">
                    (ev_charger_uid LIKE CONCAT('%', #{keyword}, '%')
                    OR ev_charger_address LIKE CONCAT('%', #{keyword}, '%'))
                </when>
                <when test="searchType == 'stationNumber'">
                    ev_charger_uid LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'address'">
                    ev_charger_address LIKE CONCAT('%', #{keyword}, '%')
                </when>
            </choose>
        </where>
    </select>

    <!-- ID로 조회 -->
    <select id="selectById" resultType="com.app.ggshop.v1.dto.EvChargerDTO">
        SELECT
            id,
            ev_charger_uid AS stationNumber,
            ev_charger_address AS installAddress,
            ev_charger_content AS chargerNote,
            ev_charger_status,
            ev_charger_mod,
            setup_date AS installDate,
            company_id AS companyId
        FROM tbl_ev_charger
        WHERE id = #{id}
    </select>

    <update id="updateEvCharger">
        UPDATE tbl_ev_charger
        SET
            ev_charger_address = #{installAddress},
            ev_charger_content = #{chargerNote},
            updated_date = NOW()
        WHERE id = #{id}
    </update>


    <delete id="deleteById">
        DELETE FROM tbl_ev_charger
        WHERE id = #{id}
    </delete>





</mapper>
