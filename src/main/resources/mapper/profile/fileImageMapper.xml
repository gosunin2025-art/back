<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.ggshop.v1.mapper.profile.FileImageMapper">
    <insert id="insertFile" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_file (file_name, file_original_name, file_size, file_path)
        values (#{fileName}, #{fileOriginalName}, #{fileSize}, #{filePath})
    </insert>


    <update id="deleteFile">
        UPDATE tbl_file
        set file_status = 'inactive'
        where id = #{id}
    </update>

    <select id="selectActiveFile">
        select * from tbl_file;
    </select>
</mapper>